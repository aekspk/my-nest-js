services:
  # app:
  #   build:
  #     context: .
  #     target: builder
  #   command: pnpm run start:dev
  #   env_file:
  #     - ./.env
  #   environment:
  #     DATABASE_URL: postgresql://myapp:mypassword@db:5432/shop?schema=public
  #   ports:
  #     - '9191:9191'
  #   depends_on:
  #     - db
  db:
    image: 'postgres:16.3-alpine3.20'
    ports:
      - 9111:5432
    environment:
      POSTGRES_USER: myapp
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: shop
    volumes:
      - ./data/pg:/var/lib/postgresql/data
  redis:
    image: redis:7.2.5-alpine3.20
    ports:
      - 6379:6379
    volumes:
      - ./redis:/data
